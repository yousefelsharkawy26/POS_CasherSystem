<Page x:Class="POS_ModernUI.Views.Pages.PurchaseManagementView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:POS_ModernUI.Views.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="clr-namespace:POS_ModernUI.Models"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Title="Purshase Management"
      d:DataContext="{d:DesignInstance local:PurchaseManagementView,
                                       IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      mc:Ignorable="d"
      FlowDirection="RightToLeft"
      xmlns:conv="clr-namespace:POS_ModernUI.Helpers">

    <Page.Resources>
        <conv:DatetoTimeAgeConverter x:Key="DatetoTimeAgeConverter" />
    </Page.Resources>
    
    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <StackPanel Height="35"
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Left"
                            Orientation="Horizontal"
                            Margin="60,0,0,2">

                    <ui:SymbolIcon Symbol="Search12"
                       Foreground="Wheat"
                       HorizontalAlignment="Left"
                       Margin="0,0,10,0"/>
                    <ui:TextBox PlaceholderText="ابحث عن المورد....."
                                Height="35"
                                Width="200"
                                Text="{Binding ViewModel.SearchSupplier, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                </StackPanel>

                <ui:Button Content="عملية شراء جديدة"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Margin="0,0,10,5"
                           Appearance="Success"
                           Foreground="WhiteSmoke"
                           FontSize="16"
                           FontWeight="DemiBold"
                           Command="{Binding ViewModel.AddPurchaseOperationCommand}"/>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.9*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
        
                <DataGrid x:Name="Workersdata"
                           ItemsSource="{Binding ViewModel.Suppliers}"
                           VerticalScrollBarVisibility="Auto"
                           CanUserReorderColumns="False"
                           CanUserResizeColumns="False"
                           AutoGenerateColumns="False"
                           CanUserResizeRows="False"
                           CanUserAddRows="False"
                           FontFamily="Calibri"
                           Margin="10 0 20 0"
                           FontSize="15"
                           Padding="7" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="المورد"
                                            Binding="{Binding Name}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width="*">
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="رقم الهاتف"
                                            Binding="{Binding ContactNumber}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width=".75*">
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="عملیات"
                                                Width="85"
                                                HeaderStyle="{DynamicResource CenterGridHeaderStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                         HorizontalAlignment="Center">
                                        <Button Margin="4"
                                                Padding="7,5"
                                                CommandParameter="{Binding}"
                                                Command="{Binding Path=DataContext.ViewModel.SelectSupplierCommand, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type DataGrid}}}">
                                            <ui:SymbolIcon Symbol="Desktop16" />
                                            <Button.ToolTip>
                                                <TextBlock Text="إظهار الطلبات"
                                            FontSize="14" />
                                            </Button.ToolTip>
                                        </Button>
                                        <Button Margin="4"
                                                Padding="7,5"
                                                CommandParameter="{Binding}"
                                                Command="{Binding Path=DataContext.ViewModel.DeleteSupplierCommand, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type DataGrid}}}">
                                            <ui:SymbolIcon Symbol="Delete16" />
                                            <Button.ToolTip>
                                                <TextBlock Text="حذف"
                                            FontSize="14" />
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
        
                <DataGrid ItemsSource="{Binding ViewModel.PurchaseOrders}"
                    VerticalScrollBarVisibility="Auto"
                    CanUserReorderColumns="False"
                    CanUserResizeColumns="False"
                    AutoGenerateColumns="False"
                    CanUserResizeRows="False"
                    CanUserAddRows="False"
                    FontFamily="Calibri"
                    Margin="10 0 10 0"
                    Grid.Column="1"
                    FontSize="15"
                    Padding="7" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الفاتورة"
                                        Binding="{Binding PurchaseOrderId}"
                                        ElementStyle="{StaticResource CenterDtaGridCell}"
                                        HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                        IsReadOnly="True"
                                        Width="*">
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="إجمالى السعر"
                                        Binding="{Binding TotalAmount}"
                                        ElementStyle="{StaticResource CenterDtaGridCell}"
                                        HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                        IsReadOnly="True"
                                        Width=".75*">
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="التاريخ"
                                        Binding="{Binding Date, Converter={StaticResource DatetoTimeAgeConverter}}"
                                        ElementStyle="{StaticResource CenterDtaGridCell}"
                                        HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                        IsReadOnly="True"
                                        Width="*">
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="عملیات"
                     Width="85"
                     HeaderStyle="{DynamicResource CenterGridHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                     HorizontalAlignment="Center">
                                    <Button Margin="4"
                                     Padding="7,5"
                                     Click="SelectRowDoubleClick_DataGrid"
                                     CommandParameter="{Binding}"
                                     Command="{Binding Path=DataContext.ViewModel.SelectPurchaseOrderCommand, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type DataGrid}}}">
                                        <ui:SymbolIcon Symbol="Desktop16" />
                                        <Button.ToolTip>
                                            <TextBlock Text="إظهار الطلبات"
                                        FontSize="14" />
                                        </Button.ToolTip>
                                    </Button>
                                    <Button Margin="4"
                                     Padding="7,5"
                                     CommandParameter="{Binding}"
                                     Command="{Binding Path=DataContext.ViewModel.DeletePurchaseOrderCommand, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type DataGrid}}}">
                                        <ui:SymbolIcon Symbol="Delete16" />
                                        <Button.ToolTip>
                                            <TextBlock Text="حذف"
                                        FontSize="14" />
                                        </Button.ToolTip>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            </Grid>
        </Grid>
        <ui:ContentDialog Title="الطلبات"
                    x:Name="dlgOrderDetails"
                    Visibility="Collapsed">
            <ui:ContentDialog.Content>
                <ui:DataGrid ItemsSource="{Binding Path=DataContext.ViewModel.PurchaseOrdersItems, UpdateSourceTrigger=PropertyChanged, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type ui:ContentDialog}}}"
                            VerticalScrollBarVisibility="Auto"
                            CanUserReorderColumns="False"
                            CanUserResizeColumns="False"
                            AutoGenerateColumns="False"
                            CanUserResizeRows="False"
                            CanUserAddRows="False"
                            FontFamily="Calibri"
                            Margin="10 0 0 20"
                            Grid.Row="2"
                            FontSize="15"
                            Padding="7">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="المنتج"
                                            Binding="{Binding Product.Name}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width="*">
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="الكمية"
                                            Binding="{Binding Quantity}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width="*">
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="السعر الواحدة"
                                            Binding="{Binding UnitCost}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width="*">
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="إجمالى السعر"
                                            Binding="{Binding SubTotal}"
                                            ElementStyle="{StaticResource CenterDtaGridCell}"
                                            HeaderStyle="{DynamicResource CenterGridHeaderStyle}"
                                            IsReadOnly="True"
                                            Width="*">
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="عملیات"
                                                Width="85"
                                                HeaderStyle="{DynamicResource CenterGridHeaderStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Center">
                                                <Button Margin="4"
                                                Padding="7,5"
                                                CommandParameter="{Binding}"
                                                Command="{Binding Path=DataContext.ViewModel.DeletePurchaseOrderItemCommand, RelativeSource= {RelativeSource FindAncestor,AncestorType={x:Type DataGrid}}}">
                                            <ui:SymbolIcon Symbol="Delete16" />
                                            <Button.ToolTip>
                                                <TextBlock Text="حذف"
                                                    FontSize="14" />
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </ui:DataGrid>
            </ui:ContentDialog.Content>
        </ui:ContentDialog>
    </Grid>
</Page>
